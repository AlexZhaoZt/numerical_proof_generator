#pragma once
#include <string>
using std::string;

void put(int * seed, int pos, int numCounter, int symCounter);
bool checker(int * seed);
void print(int * seed);
string translate(string input);
